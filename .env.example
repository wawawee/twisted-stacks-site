# GitHub PAT with repo write (wawawee/ATE, wawawee/VR-SuperPowers)
GITHUB_TOKEN_WAWAWEE=

# Vercel CLI only — do NOT use as SUPABASE_SERVICE_ROLE_KEY
VERCEL_TOKEN=

# Supabase showroom — project oeppjpbaafjsowecyjho
SUPABASE_URL=https://oeppjpbaafjsowecyjho.supabase.co
# Dashboard → Settings → API: `sb_secret_*` or legacy service_role JWT
SUPABASE_SERVICE_ROLE_KEY=
# Optional alias (API also reads this if SERVICE_ROLE_KEY unset)
# SUPABASE_SECRET_KEY=

# Browser client (publishable key only — never sb_secret_ or service_role)
VITE_SUPABASE_URL=https://oeppjpbaafjsowecyjho.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional override for the browser leaderboard client. Defaults to /api/leaderboard.
VITE_LEADERBOARD_API_PATH=

# --- Contact form (Vercel /api/contact) ---
RESEND_API_KEY=
CONTACT_FROM_EMAIL=TwistedStacks <hello@twistedstacks.com>
CONTACT_TO_EMAIL=hello@twistedstacks.com
CONTACT_NOTIFY_EMAIL=dev@twistedstacks.com

# --- SUPARAYS team room (/suparays) ---
SUPARAYS_ROOM_PASSWORD=baha123
# SUPARAYS_ROOM_SECRET=

# --- ATE investor colab (/ate) ---
ATE_ROOM_PASSWORD=baha123
# ATE_ROOM_SECRET=
# ATE_REPO_LOCAL=/absolute/path/to/ATE
# HITL → ATE FastAPI proxy (when TEMPORAL_* set on Vercel)
# TEMPORAL_ADDRESS=localhost:7233
# TEMPORAL_NAMESPACE=default
# ATE_HITL_PROXY_URL=http://localhost:8000/hitl/signal
